import React from 'react'
import ReactDOM from 'react-dom'
import App from './components/App'

// 1 INSTALL json server
// https://www.npmjs.com/package/json-server
// npm i -g  json-server => in case you have a json db provided
// json-server --watch db.json --port 3004 => run json server

// 2 INSTALL REACT ROUTER
// https://www.npmjs.com/package/react-router-dom
// npm install --save react-router-dom

// 3 INSTALL SLIDER
// https://www.npmjs.com/package/react-slick
// npm install react-slick --save

// 4 INSTALL REACT REVEAL
// https://www.npmjs.com/package/react-reveal
// npm install react-reveal --save

// 5 INSTALL REACT TRANSITION GROUP

// https://www.npmjs.com/package/react-transition-group
// npm install react-transition-group --save


ReactDOM.render(<App/>, document.getElementById('root'))